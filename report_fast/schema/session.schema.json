{
  "$defs": {
    "BackgroundItem": {
      "properties": {
        "croppingContext": {
          "properties": {
            "region": {},
            "transform": {}
          },
          "required": [
            "region",
            "transform"
          ],
          "type": "object",
          "x-description": "Present on an expanded child: the crop + alignment this child renders. Read by the open pipeline to crop the child's visualization data layers to the same region as its background image."
        },
        "dataReference": {},
        "id": {
          "type": "string"
        },
        "microns": {
          "type": "number"
        },
        "micronsX": {
          "type": "number"
        },
        "micronsY": {
          "type": "number"
        },
        "name": {
          "type": "string"
        },
        "options": {
          "properties": {
            "format": {
              "type": "string"
            }
          },
          "type": "object",
          "x-description": "Ggeneric value map, where some values are already pre-defined: @property format the desired format to use, can be arbitrary but when sources can, it's optimal to support browser-standard values like png, tiff, jpeg/jpg"
        },
        "protocol": {
          "type": "string"
        },
        "sessionName": {
          "type": "string"
        },
        "shaders": {
          "items": {},
          "type": "array"
        },
        "virtualOf": {
          "type": "string",
          "x-description": "Present on an *expanded child*: the id of the parent background it was derived from. Used to detect already-expanded children (idempotency) and to group siblings."
        },
        "virtualization": {
          "x-description": "Present on a *parent* background that owns a stored decomposition (probe-then-persist). Drives `expandVirtualBackgrounds` to materialize first-class child backgrounds, one per region."
        },
        "virtualizationMode": {
          "x-description": "Render policy for a virtualized parent. Runtime-switchable via `setVirtualizationMode`; defaults to `\"none\"`."
        },
        "visualizationIndex": {
          "nullable": true,
          "type": "number"
        }
      },
      "required": [
        "dataReference"
      ],
      "type": "object",
      "x-description": "@property dataReference index to the `data` array, can be only one unlike in `shaders`, required - marks the target data item others refer to (e.g. in measurements) @property shaders array of optional rendering specification @property protocol deprecated on background object. Name of a protocol registered in `window.SLIDE_PROTOCOLS`. In non-secure mode the value may also be a raw backtick-template URL string (legacy compatibility, discouraged). @property name custom tissue name, default the tissue path @property sessionName overrides the global params.sessionName for this background @property visualizationIndex the per-background visualization selection \u2014 the index into `config.visualizations` that this background renders. Authoritative source of truth for per-viewer viz binding; viewer slot k displays `config.background[activeBackgroundIndex[k]].visualizationIndex`. `null` means \"no overlay for this background\". Slot reordering / insertion / deletion preserves the bg\u2192viz binding. @property id unique ID for the background, created automatically from data path if not defined",
      "x-kind": "interface"
    },
    "DataID": {
      "additionalProperties": {},
      "type": [
        "string",
        "object"
      ],
      "x-kind": "type",
      "x-ts": "string | Record<string, any>"
    },
    "DataOverride": {
      "properties": {
        "croppingContext": {
          "properties": {
            "region": {},
            "transform": {}
          },
          "required": [
            "region",
            "transform"
          ],
          "type": "object",
          "x-description": "Present when this spec resolves a *virtual* (cropped) source via the `virtual-region` protocol. Carries the crop + alignment so the `virtual-region` factory can wrap the parent (resolved from `dataID`). Threaded onto BOTH a virtual child's background image spec and \u2014 at resolution time \u2014 every visualization data layer of that child, so the whole co-registered stack crops together. See the virtual-viewports plan."
        },
        "dataID": {
          "additionalProperties": {},
          "type": [
            "string",
            "object"
          ]
        },
        "imageSmoothingEnabled": {
          "type": "boolean",
          "x-description": "By default enabled, allows turning off data sampling interpolation."
        },
        "microns": {
          "type": "number"
        },
        "micronsX": {
          "type": "number"
        },
        "micronsY": {
          "type": "number"
        },
        "options": {
          "properties": {
            "format": {
              "type": "string"
            }
          },
          "type": "object",
          "x-description": "Ggeneric value map, where some values are already pre-defined: @property format the desired format to use, can be arbitrary but when sources can, it's optimal to support browser-standard values like png, tiff, jpeg/jpg"
        },
        "protocol": {
          "type": "string"
        },
        "tileSource": {}
      },
      "required": [
        "dataID"
      ],
      "type": "object",
      "x-description": "A more holistic data specification, which can provide custom options for the target protocol (underlying TileSource API), and override the default fetching behavior (e.g. to use a custom data source). @property dataID actual data value, required - its presence is used to identify this object is DataOverride type @property options passed to the data source integration logics - TileSource class @property microns size of pixel in micrometers, default `undefined`, @property micronsX horizontal size of pixel in micrometers, default `undefined`, if general value not specified must have both X,Y @property micronsY vertical size of pixel in micrometers, default `undefined`, if general value not specified must have both X,Y @property protocol Name of a protocol registered in `window.SLIDE_PROTOCOLS` (always safe, including secure mode). In non-secure mode the value may alternatively be a raw backtick-template URL string (legacy compatibility, discouraged) \u2014 rejected in secure mode with a warning. @property tileSource DEPRECATED: a pre-built tileSource object. Kept for one deprecation cycle; plugins should register a factory protocol with `SLIDE_PROTOCOLS.register({ id, createTileSource })` and reference it via `protocol` instead. The pre-built TileSource is not serializable and breaks URL/POST roundtripping.",
      "x-kind": "interface"
    },
    "DataSpecification": {
      "x-kind": "type",
      "x-ts": "DataID | DataOverride"
    },
    "VisualizationItem": {
      "properties": {
        "goalIndex": {
          "type": "number"
        },
        "name": {
          "type": "string"
        },
        "protocol": {
          "type": "string"
        },
        "shaders": {
          "additionalProperties": {},
          "type": "object"
        }
      },
      "required": [
        "shaders"
      ],
      "type": "object",
      "x-description": "@property shaders array of shader specifications @property protocol deprecated on visualization object.  Name of a protocol registered in `window.SLIDE_PROTOCOLS`. In non-secure mode the value may also be a raw backtick-template URL string (legacy compatibility, discouraged). @property name custom tissue name, default the tissue path @property goalIndex preferred visualization index when this item is selected",
      "x-kind": "interface"
    },
    "VisualizationShaderGroup": {
      "properties": {
        "order": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "shaders": {
          "additionalProperties": {},
          "type": "object"
        },
        "type": {
          "enum": [
            "group"
          ]
        }
      },
      "required": [
        "type"
      ],
      "type": "object",
      "x-kind": "interface"
    },
    "VisualizationShaderLayer": {
      "properties": {
        "dataReferences": {
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "tiledImages": {
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "type": {
          "type": "string"
        }
      },
      "type": "object",
      "x-kind": "interface"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "The JSON the viewer parses out of the URL fragment. Generated from the viewer source by scripts/derive_schema.py -- do not hand-edit. Keys an interface declares with `[key: string]: any` are open by design: the viewer passes them through to plugins and layer constructors.",
  "properties": {
    "background": {
      "items": {
        "properties": {
          "croppingContext": {
            "properties": {
              "region": {},
              "transform": {}
            },
            "required": [
              "region",
              "transform"
            ],
            "type": "object",
            "x-description": "Present on an expanded child: the crop + alignment this child renders. Read by the open pipeline to crop the child's visualization data layers to the same region as its background image."
          },
          "dataReference": {},
          "id": {
            "type": "string"
          },
          "microns": {
            "type": "number"
          },
          "micronsX": {
            "type": "number"
          },
          "micronsY": {
            "type": "number"
          },
          "name": {
            "type": "string"
          },
          "options": {
            "properties": {
              "format": {
                "type": "string"
              }
            },
            "type": "object",
            "x-description": "Ggeneric value map, where some values are already pre-defined: @property format the desired format to use, can be arbitrary but when sources can, it's optimal to support browser-standard values like png, tiff, jpeg/jpg"
          },
          "protocol": {
            "type": "string"
          },
          "sessionName": {
            "type": "string"
          },
          "shaders": {
            "items": {},
            "type": "array"
          },
          "virtualOf": {
            "type": "string",
            "x-description": "Present on an *expanded child*: the id of the parent background it was derived from. Used to detect already-expanded children (idempotency) and to group siblings."
          },
          "virtualization": {
            "x-description": "Present on a *parent* background that owns a stored decomposition (probe-then-persist). Drives `expandVirtualBackgrounds` to materialize first-class child backgrounds, one per region."
          },
          "virtualizationMode": {
            "x-description": "Render policy for a virtualized parent. Runtime-switchable via `setVirtualizationMode`; defaults to `\"none\"`."
          },
          "visualizationIndex": {
            "nullable": true,
            "type": "number"
          }
        },
        "required": [
          "dataReference"
        ],
        "type": "object",
        "x-description": "@property dataReference index to the `data` array, can be only one unlike in `shaders`, required - marks the target data item others refer to (e.g. in measurements) @property shaders array of optional rendering specification @property protocol deprecated on background object. Name of a protocol registered in `window.SLIDE_PROTOCOLS`. In non-secure mode the value may also be a raw backtick-template URL string (legacy compatibility, discouraged). @property name custom tissue name, default the tissue path @property sessionName overrides the global params.sessionName for this background @property visualizationIndex the per-background visualization selection \u2014 the index into `config.visualizations` that this background renders. Authoritative source of truth for per-viewer viz binding; viewer slot k displays `config.background[activeBackgroundIndex[k]].visualizationIndex`. `null` means \"no overlay for this background\". Slot reordering / insertion / deletion preserves the bg\u2192viz binding. @property id unique ID for the background, created automatically from data path if not defined"
      },
      "type": "array"
    },
    "data": {
      "items": {},
      "type": "array"
    },
    "params": {
      "properties": {
        "activeBackgroundIndex": {
          "type": [
            "array",
            "number"
          ]
        },
        "activeVisualizationIndex": {
          "type": [
            "array",
            "number"
          ],
          "x-deprecated": true,
          "x-description": "@deprecated Per-viewer visualization is now a property of each background entry (`config.background[i].visualizationIndex`). Legacy input shape is still accepted \u2014 it is folded into bg entries at config-parse time \u2014 but no longer authoritative state and never emitted on serialization."
        },
        "background": {
          "nullable": true,
          "type": "string"
        },
        "backgroundColor": {
          "nullable": true,
          "type": "string"
        },
        "branding": {
          "nullable": true,
          "properties": {
            "appleTouchIcon": {
              "nullable": true,
              "type": "string"
            },
            "icon16": {
              "nullable": true,
              "type": "string"
            },
            "icon32": {
              "nullable": true,
              "type": "string"
            },
            "logo": {
              "nullable": true,
              "type": "string"
            },
            "maskIcon": {
              "nullable": true,
              "type": "string"
            },
            "maskIconColor": {
              "nullable": true,
              "type": "string"
            },
            "title": {
              "nullable": true,
              "type": "string"
            }
          },
          "type": "object",
          "x-description": "Operator-trusted custom branding (ENV `core.setup.branding` only \u2014 read via `APPLICATION_CONTEXT.defaultParams`, never `getOption`, so an imported session / URL param cannot override it; see AGENTS.md \u00a77). `title` and the favicon paths are consumed server-side when rendering the page head; `logo` renders a company image at the left of the top app bar. Any key omitted falls back to the stock xOpat asset."
        },
        "bypassCache": {
          "nullable": true,
          "type": "boolean"
        },
        "bypassCacheLoadTime": {
          "nullable": true,
          "type": "boolean"
        },
        "bypassCloseConfirmation": {
          "nullable": true,
          "type": "boolean"
        },
        "bypassCookies": {
          "nullable": true,
          "type": "boolean"
        },
        "captureIndicator": {
          "enum": [
            "flash",
            "off",
            "trail"
          ],
          "x-description": "Visual marker drawn on a viewer whenever something reads pixels out of it (off-screen region render, viewport grab \u2014 see the `region-capture` event). `\"trail\"` (default) accumulates markers for the duration of a run so the analyzed parts of a slide stay visible while it works; `\"flash\"` shows only the capture in flight; `\"off\"` renders nothing. A user preference, not a security control."
        },
        "captureIndicatorIdleMs": {
          "nullable": true,
          "type": "number",
          "x-description": "Milliseconds after the last capture finishes before the drawn markers are removed (default 6000). Only affects what is displayed \u2014 `captureIndicator.getLog()` keeps the history regardless."
        },
        "customBlending": {
          "nullable": true,
          "type": "boolean"
        },
        "debugMode": {
          "nullable": true,
          "type": "boolean"
        },
        "disablePluginsAutoload": {
          "nullable": true,
          "type": "boolean",
          "x-description": "If true, skip the cookie-driven plugin restore (`_plugins`) for this session. Plugins flagged `permaLoad: true` in their `include.json` and plugins explicitly listed in this session's `config.plugins` still come up normally. The intent is to ignore the user's *previous* manual picks while still respecting the deployment's auto-loaded set and the current session's declared plugins."
        },
        "disablePluginsUi": {
          "nullable": true,
          "type": "boolean",
          "x-description": "Hide the plugin catalogue \u2014 the listing that browses available plugins and loads new ones. Loaded plugins are unaffected: their menu rows, fullscreen settings tabs and view panels render as usual."
        },
        "fetchAsync": {
          "nullable": true,
          "type": "boolean"
        },
        "grayscale": {
          "nullable": true,
          "type": "boolean"
        },
        "historySize": {
          "nullable": true,
          "type": "number"
        },
        "isStaticPreview": {
          "nullable": true,
          "type": "boolean"
        },
        "kineticPan": {
          "nullable": true,
          "type": "boolean",
          "x-description": "If true (default), releasing a fast drag lets the slide coast to a stop (momentum). The drag itself remains strictly 1:1 with the cursor."
        },
        "kineticPanFriction": {
          "nullable": true,
          "type": "number",
          "x-description": "Velocity retained per 1/60 s of coasting, in `(0, 1)`. Default `0.92`; lower values stop the slide sooner."
        },
        "kineticPanMinSpeed": {
          "nullable": true,
          "type": "number"
        },
        "locale": {
          "nullable": true,
          "type": "string"
        },
        "maxImageCacheCount": {
          "nullable": true,
          "type": "number",
          "x-description": "Per-viewer OSD tile-cache budget (number of tile records). `null` (default) = adaptive: scaled from the display's device-pixel area against a 1080p\u21921200 baseline, softly split across open viewports and clamped per device class (desktop [1000,4000], mobile [600,1500]). A number pins a fixed per-viewer budget and overrides the adaptive value. See `src/classes/app/osd-performance.ts`."
        },
        "maxMobileWidthPx": {
          "nullable": true,
          "type": "number"
        },
        "notificationsPosition": {
          "enum": [
            "bottom",
            "top"
          ],
          "x-description": "Vertical placement of the system notification toast (`window.Dialogs`). Default `\"bottom\"`. Set to `\"top\"` for embeddings where the bottom edge is reserved (e.g. notebook hosts with their own status bar). Runtime changes via `Dialogs.setPosition(...)` persist on this same key."
        },
        "permaLoadPlugins": {
          "nullable": true,
          "type": "boolean"
        },
        "preventNavigationShortcuts": {
          "nullable": true,
          "type": "boolean"
        },
        "quickActions": {
          "items": {
            "properties": {
              "icon": {
                "nullable": true,
                "type": "string"
              },
              "id": {
                "type": "string"
              },
              "label": {
                "nullable": true,
                "type": "string"
              }
            },
            "required": [
              "id"
            ],
            "type": [
              "string",
              "object"
            ]
          },
          "nullable": true,
          "type": "array",
          "x-description": "Fast-access icon-only actions pinned into the top app bar, referenced by `AppBar.Actions` catalogue key \u2014 `\"tools:core.sync.auto\"`, `\"view:sideViewerMenu.navigator\"`, `\"shortcut:<id>\"`, `\"custom:<id>\"`. Two trust tiers. ENV (`core.setup`, read via `defaultParams`) is operator-trusted: entries may be objects overriding `icon`/`label`. The per-user list resolved through `getOption` is session-controllable (URL params, an imported peer session \u2014 AGENTS.md \u00a77) and therefore accepts id strings only**; object entries there are reduced to their `id`, so a hostile bundle cannot relabel one action to impersonate another (or point `icon` at a remote URL). Unresolvable ids are dropped."
        },
        "quickActionsMaxVisible": {
          "nullable": true,
          "type": "number",
          "x-description": "How many pins render as buttons before the remainder spills into the trailing overflow menu. Default 5 \u2014 the app bar is 35px tall and shares its width with the toolbar embed slot."
        },
        "quickActionsUserEditable": {
          "nullable": true,
          "type": "boolean",
          "x-description": "Operator lock. `false` freezes the bar at the ENV list and hides the Settings card. Read from `defaultParams` ONLY, never `getOption`."
        },
        "requestSchedulerBgBusy": {
          "nullable": true,
          "type": "number",
          "x-description": "Same cap while any viewer IS loading tiles \u2014 background hard-yields so tiles take the whole connection pool. Default 0. (Starvation escape below keeps background from freezing.)"
        },
        "requestSchedulerBgIdle": {
          "nullable": true,
          "type": "number",
          "x-description": "Max concurrent `priority:\"background\"` HTTP requests per origin (inference, transcription, z-plane prefetch) while NO viewer is loading tiles. Default 2. See `src/classes/app/request-scheduler.ts`."
        },
        "requestSchedulerMaxStarveMs": {
          "nullable": true,
          "type": "number",
          "x-description": "A queued background request waiting at least this long is admitted even while tiles load (one at a time), so dictation/drafting never freezes under sustained navigation. Default 1500 (ms)."
        },
        "reverseScroll": {
          "nullable": true,
          "type": "boolean",
          "x-description": "If true, the scroll-to-zoom direction is inverted: scrolling down zooms in and scrolling up zooms out. Intended for users who expect map-style or trackpad-style wheel behaviour."
        },
        "scaleBar": {
          "nullable": true,
          "type": "boolean"
        },
        "scrollPixelsPerNotch": {
          "nullable": true,
          "type": "number",
          "x-description": "Wheel pixels that constitute one full zoom step (default `120`, the conventional mouse notch). Trackpads emit much smaller deltas and are therefore applied fractionally, which is what keeps them smooth without any event throttling. Lower this to make trackpads more aggressive."
        },
        "scrollRequiresCtrl": {
          "nullable": true,
          "type": "boolean",
          "x-description": "If true, the viewer only zooms on `Ctrl/Cmd + wheel`; plain wheel scrolls the host page through. Intended for notebook / scrollable-host embeddings where unintentional viewer zoom hijacks page scroll."
        },
        "scrollSpeed": {
          "nullable": true,
          "type": "number",
          "x-description": "Multiplier on the normalized wheel delta (default `1`). Values above 1 cover more zoom range per wheel turn, below 1 tame an over-sensitive device. Applies to continuous zoom, magnification snapping and Alt+wheel z-stack scrubbing alike."
        },
        "sessionName": {
          "nullable": true,
          "type": "string"
        },
        "snapZoomToMagnification": {
          "nullable": true,
          "type": "boolean",
          "x-description": "If true (default), scroll-to-zoom snaps between standard objective magnification stops (5x/10x/20x/40x\u2026) \u2014 but only when the current slide has a resolved native magnification (a calibrated MPP). Uncalibrated / pixel-unit slides fall back to continuous zoom. Composes with `reverseScroll`."
        },
        "statusBar": {
          "nullable": true,
          "type": "boolean"
        },
        "theme": {
          "nullable": true,
          "type": "string"
        },
        "tileCache": {
          "nullable": true,
          "type": "boolean"
        },
        "toolBar": {
          "nullable": true,
          "type": "boolean"
        },
        "ui": {
          "properties": {
            "appBar": {
              "nullable": true,
              "type": "boolean",
              "x-description": "Initial visible state of the top app bar chrome. `false` is equivalent to the hide-UI button being pre-toggled at boot \u2014 hides every Chrome-registered component until the user toggles back."
            },
            "globalMenu": {
              "nullable": true,
              "type": "boolean",
              "x-description": "Initial visible state of the global right-side dock (`window.LAYOUT`, `MainLayout`) that hosts plugin tabs such as chats, slide-switcher and questionnaire. `false` boots the dock closed; the user (and plugins that explicitly focus a tab) can still reopen it."
            },
            "globalMenuMode": {
              "enum": [
                "docked",
                "overlay"
              ],
              "x-description": "Interaction mode of the global right-side dock. `\"overlay\"` (default): the dock hides to a thin edge rail and floats over the viewer on hover/focus (no viewer reflow). `\"docked\"`: the dock is a flex sibling that pushes the viewer and stays open when open (the classic behavior). Unlike the boolean flags above this is NOT read via `getUiOption` (which is boolean-only) \u2014 `MainLayout` reads it directly at construction. The user's runtime pin choice (AppCache `<layoutId>-dock-mode`) overrides this session/deployment default."
            },
            "mainMenu": {
              "nullable": true,
              "type": "boolean"
            },
            "navigator": {
              "nullable": true,
              "type": "boolean"
            },
            "scaleBar": {
              "nullable": true,
              "type": "boolean"
            },
            "sideMenuCompact": {
              "nullable": true,
              "type": "boolean",
              "x-description": "Compact mode of the per-viewer right-side menu tab strips: icon-only strips whose sideways title reveals on hover, occupying much less space. Unlike the boolean visibility flags above this defaults to `false` and is therefore NOT read via `getUiOption` (which defaults unset keys to `true`) \u2014 see `resolveSideMenuCompact` in `ui/classes/components/rightSideViewerMenu.mjs`. The user's Settings toggle persists to AppCache and this session param overrides it."
            },
            "statusBar": {
              "nullable": true,
              "type": "boolean"
            },
            "toolBar": {
              "nullable": true,
              "type": "boolean"
            }
          },
          "type": [
            "boolean",
            "object"
          ],
          "x-description": "Canonical home for UI initial-visibility flags. See `XOpatUiSetup`. As a shorthand, set to `false` to hide every global UI component at boot (useful for headless / notebook embeddings). `true` is equivalent to leaving the field unset."
        },
        "valueInspectorEnabled": {
          "nullable": true,
          "type": "boolean"
        },
        "viewport": {
          "properties": {
            "point": {
              "properties": {
                "x": {
                  "type": "number"
                },
                "y": {
                  "type": "number"
                }
              },
              "required": [
                "x",
                "y"
              ],
              "type": "object"
            },
            "rotation": {
              "type": "number"
            },
            "zoomLevel": {
              "type": "number"
            }
          },
          "required": [
            "point",
            "zoomLevel"
          ],
          "type": [
            "array",
            "object"
          ]
        },
        "visualizationInspectorEnabled": {
          "nullable": true,
          "type": "boolean"
        },
        "visualizationInspectorLensZoom": {
          "nullable": true,
          "type": "number"
        },
        "visualizationInspectorMode": {
          "nullable": true,
          "type": "string"
        },
        "visualizationInspectorRadiusPx": {
          "nullable": true,
          "type": "number"
        },
        "webGlPreferredVersion": {
          "nullable": true,
          "type": "string"
        },
        "webglDebugMode": {
          "nullable": true,
          "type": "boolean"
        },
        "zPlaneCacheEnabled": {
          "nullable": true,
          "type": "boolean",
          "x-description": "Keep visited/prefetched focal planes of a z-stack as extra per-tile OSD cache records so plane revisits are served without a network round-trip. Default `true`. `false` restores fetch-per-scrub behavior."
        },
        "zPlaneCacheMaxItems": {
          "nullable": true,
          "type": "number",
          "x-description": "Budget for the z-plane cache records (they also count toward `maxImageCacheCount`). Oldest records are dropped first. Default 400."
        },
        "zPrefetchConcurrency": {
          "nullable": true,
          "type": "number",
          "x-deprecated": true,
          "x-description": "@deprecated No-op. Prefetch concurrency is now bounded globally by `APPLICATION_CONTEXT.requestScheduler` (background lane, per tile origin, shared across viewers). Use `requestSchedulerBgIdle`/`requestSchedulerBgBusy`."
        },
        "zPrefetchRadius": {
          "nullable": true,
          "type": "number",
          "x-description": "After a plane change settles, prefetch the `z\u00b11..radius` variants of the tiles currently in the viewport. `0` disables prefetching. Default 1."
        },
        "zRepaintOffViewport": {
          "enum": [
            "cached-only",
            "fetch"
          ],
          "x-description": "What happens to loaded tiles OUTSIDE the viewport on a plane change: `\"cached-only\"` (default) swaps only planes already in the cache and unloads the rest (they reload at the live plane when panned back to); `\"fetch\"` refetches every loaded tile over the network (full fidelity)."
        }
      },
      "type": "object"
    },
    "plugins": {
      "description": "Plugin id -> options, loaded on boot.",
      "type": "object"
    },
    "visualizations": {
      "items": {
        "properties": {
          "goalIndex": {
            "type": "number"
          },
          "name": {
            "type": "string"
          },
          "protocol": {
            "type": "string"
          },
          "shaders": {
            "additionalProperties": {},
            "type": "object"
          }
        },
        "required": [
          "shaders"
        ],
        "type": "object",
        "x-description": "@property shaders array of shader specifications @property protocol deprecated on visualization object.  Name of a protocol registered in `window.SLIDE_PROTOCOLS`. In non-secure mode the value may also be a raw backtick-template URL string (legacy compatibility, discouraged). @property name custom tissue name, default the tissue path @property goalIndex preferred visualization index when this item is selected"
      },
      "type": "array"
    }
  },
  "title": "xOpat v3 session",
  "type": "object",
  "x-note": "Top-level keys outside `properties` are not an error -- the viewer forwards unknown config keys to plugins. They are a smell, not a fault; params keys and shader control params are the closed sets."
}
